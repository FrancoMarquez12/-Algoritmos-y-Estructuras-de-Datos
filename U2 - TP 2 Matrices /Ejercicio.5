using System;

class Program
{
    static void Main()
    {
        int[,] miMatriz = {
            { 1, 2, 3 },
            { 4, 99, 6 },
            { 7, 8, 9 }
        };

        Console.WriteLine("Fila del maximo absoluto: " + FilaMaximoAbsoluto(miMatriz));
    }

    static int FilaMaximoAbsoluto(int[,] matriz)
    {
        int maxElemento = matriz[0, 0];
        int idxFila = 0;

        int filas = matriz.GetLength(0);
        int columnas = matriz.GetLength(1);

        for (int i = 0; i < filas; i++)
        {
            for (int j = 0; j < columnas; j++)
            {
                if (matriz
