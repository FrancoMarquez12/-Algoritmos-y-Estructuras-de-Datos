using System;

class Program
{
    static void Main()
    {
        int[,] miMatriz = {
            { 1, 5, 9 },
            { 1, 5, 9 }
        };

        int[,] resultado = IntercambiarColumnas(miMatriz, 0, 2);

        Console.WriteLine("Matriz con columnas 0 y 2 cambiadas:");
        ImprimirMatriz(resultado);
    }

    static int[,] IntercambiarColumnas(int[,] matriz, int col1, int col2)
    {
        int filas = matriz.GetLength(0);

        for (int i = 0; i < filas; i++)
        {
            int temp = matriz[i, col1];
            matriz[i, col1] =
