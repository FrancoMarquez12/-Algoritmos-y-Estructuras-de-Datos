using System;

class Program
{
    static void Main()
    {
        int[,] miMatriz = {
            { 1, 1, 1 },
            { 2, 2, 2 },
            { 3, 3, 3 }
        };

        int[,] resultado = IntercambiarFilas(miMatriz, 0, 2);

        Console.WriteLine("Matriz con filas 0 y 2 cambiadas:");
        ImprimirMatriz(resultado);
    }

    static int[,] IntercambiarFilas(int[,] matriz, int fila1, int fila2)
    {
        int columnas = matriz.GetLength(1);

        for (int j = 0; j < columnas; j++)
        {
            int temp = matriz[fila1, j];
            matriz
