using System;

class Program
{
    static void Main()
    {
        int[,] miMatriz = {
            { 1, 2, 3 },
            { 4, 99, 6 },
            { 7, 8, 9 }
        };

        var pos = PosicionMaximoAbsoluto(miMatriz);
        Console.WriteLine($"Posicion (fila, col) del maximo: ({pos.fila}, {pos.col})");
    }

    static (int fila, int col) PosicionMaximoAbsoluto(int[,] matriz)
    {
        int maxElemento = matriz[0, 0];
        (int fila, int col) posicion = (0, 0);

        int filas = matriz.GetLength(0);
        int columnas = matriz.GetLength(1);

        for (int i = 0; i < filas; i++)
        {
            for (int
